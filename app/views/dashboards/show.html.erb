<br />
<div class = "booyah-box">
  <br />
  <div class = "row">
    <div class = "col-4 text-center">
      <%= image_tag 'user.png' %>
      <br /><br />
      <%= link_to 'Sing Out', destroy_user_session_path, method: :delete, class: "btn btn-warning" %>
    </div>
    <div class = "col-8">
      <br />
      <h1>Welcome, <%= @user.first_name %> to your account!</h1>
      <h3>Member Since: <%= @user.created_at.strftime("%B %d, %Y") %></h3>
      <h4><%= @user.comments.count %> total comments</h4>
      <h4><%= @user.comments.where(rating: '5_stars').count %> total 5 star reviews</h4>
    </div>
  </div>
  <br />
</div>
<br /><br />
<div class = "text-center">
  <%= link_to 'Add a new Game to database', new_game_path, class: 'btn btn-secondary waves-effect waves-light' %>
</div>
<br /><hr /><br />

<h1 class = "text-center">My Comments</h1>
<br /><br />
<div class = "row">
  <% @user.comments.each do |comment| %>
    <div class="col-12 col-sm-6">
      <h2 class = "text-center"><%= link_to comment.game.title, game_path(comment.game) %></h2>
      <blockquote>
        <% if comment.humanized_rating.present? %>
          <b><%= comment.humanized_rating %></b> -
        <% end %>
        <%= comment.message %>
      </blockquote>
    </div>
  <% end %>
</div>